# 计网与操作系统八股

> 整理日期：2026-09-10
> 上半部分：计算机网络；下半部分：操作系统

---

# 第一部分：计算机网络

## 一、OSI 七层模型

**OSI 七层模型** 是国际标准化组织提出的一个网络分层模型：

![OSI 七层模型](https://oss.javaguide.cn/github/javaguide/cs-basics/network/osi-7-model.png)

每一层都专注做一件事情，并且每一层都需要使用下一层提供的功能。比如传输层需要使用网络层提供的路由和寻址功能，这样传输层才知道把数据传输到哪里去。

**TCP/IP 四层模型** 是目前被广泛采用的一种模型，可以看作是 OSI 七层模型的精简版本，由以下 4 层组成：

1. 应用层
2. 传输层
3. 网络层
4. 网络接口层

需要注意的是，我们并不能将 TCP/IP 四层模型和 OSI 七层模型完全精确地匹配起来，不过可以简单将两者对应起来，如下图所示：

![TCP/IP 四层模型](https://oss.javaguide.cn/github/javaguide/cs-basics/network/tcp-ip-4-model.png)

---

## 二、TCP 三次握手和四次挥手

### 三次握手

建立一个 TCP 连接需要"三次握手"，缺一不可：

1. **第一次握手 (SYN)**：客户端向服务端发送一个 SYN 报文段，其中包含一个由客户端随机生成的初始序列号（ISN），例如 `seq=x`。发送后，客户端进入 **SYN_SENT** 状态，等待服务端的确认。

2. **第二次握手 (SYN+ACK)**：服务端收到 SYN 报文段后，如果同意建立连接，会向客户端回复一个确认报文段。该报文段包含两个关键信息：

   - **SYN**：服务端也需要同步自己的初始序列号，例如 `seq=y`。
   - **ACK**：用于确认收到了客户端的请求，确认号为客户端初始序列号加一，即 `ack=x+1`。
   - 发送该报文段后，服务端进入 **SYN_RCVD** 状态。

3. **第三次握手 (ACK)**：客户端收到服务端的 SYN+ACK 报文段后，会向服务端发送一个最终的确认报文段，包含确认号 `ack=y+1`。发送后，客户端进入 **ESTABLISHED** 状态。服务端收到这个 ACK 后，也进入 **ESTABLISHED** 状态。

### 四次挥手

断开一个 TCP 连接则需要"四次挥手"，缺一不可：

1. **第一次挥手 (FIN)**：客户端决定关闭连接，向服务端发送 FIN 报文段，表示自己已经没有数据要发送了，序列号 `seq=u`。发送后，客户端进入 **FIN-WAIT-1** 状态。

2. **第二次挥手 (ACK)**：服务端收到 FIN 后，立即回复 ACK 确认报文段，确认号为 `ack=u+1`。发送后，服务端进入 **CLOSE-WAIT** 状态。客户端收到 ACK 后进入 **FIN-WAIT-2** 状态。此时 TCP 连接处于**半关闭（Half-Close）**状态：客户端到服务端的发送通道已关闭，但服务端仍可以发送数据。

3. **第三次挥手 (FIN)**：服务端确认所有数据发送完毕后，向客户端发送 FIN 报文段，序列号 `seq=y`。发送后，服务端进入 **LAST-ACK** 状态。

4. **第四次挥手 (ACK)**：客户端收到 FIN 后，回复 ACK 确认报文段，确认号为 `ack=y+1`。发送后，客户端进入 **TIME-WAIT** 状态。服务端收到 ACK 后立即进入 **CLOSED** 状态。客户端在 **TIME-WAIT** 状态下等待 **2MSL** 后，才最终进入 **CLOSED** 状态。

---

## 三、TCP 和 UDP 区别

| 维度 | TCP（Transmission Control Protocol） | UDP（User Datagram Protocol） |
|---|---|---|
| 是否连接 | 面向连接：三次握手建立、四次挥手释放 | 无连接：直接发送报文 |
| 可靠性 | 序列号 + ACK + 重传，可靠、有序、不丢不重 | Best-Effort：可能丢包、乱序、重复，不保证送达 |
| 传输粒度 | 字节流（Stream），应用层看不到分段 | 报文（Datagram），一次 `send()` 对应一次完整包 |
| 流量 / 拥塞控制 | 有窗口和拥塞算法，如慢启动、拥塞避免、BBR | 无流控、无拥塞控制，发得快可能丢得更多 |
| 首部开销 | 20 Byte 起，带选项可到 40-60 Byte | 固定 8 Byte，结构简单 |
| 传输速度 | 稳定，但握手和拥塞控制导致首包较慢 | 低延迟、抖动小，但需应用层自行容错 |
| 组播 / 广播 | 不支持 | 原生支持单播、组播、广播 |
| 适用场景 | HTTP/HTTPS、FTP、数据库、电子邮件等 | DNS、VoIP、直播、在线游戏、DHCP 等 |
| 稳定性成本 | 端口占用较多、FD 常驻、握手耗 RTT | 资源占用低、无状态，服务端易于高并发 |
| 消息边界 | 无，需要应用层自行划分消息边界 | 天然保留消息边界，一包一消息 |

---

## 四、TCP 粘包与拆包

```text
原因：TCP 是字节流协议，没有消息边界

粘包：多个小包合并发送，接收方一次收到多条消息
拆包：一个大包被拆分，接收方一次只收到半条

解决方案：
1. 固定长度消息
2. 消息头带长度字段（最常用）
3. 特殊分隔符
4. 应用层协议（HTTP 的 Content-Length、RPC 协议头）
```

---

## 五、TCP 拥塞控制

```text
四种算法：
1. 慢启动：窗口从 1 指数增长
2. 拥塞避免：达到阈值后线性增长
3. 快重传：收到 3 个重复 ACK 立即重传
4. 快恢复：快重传后窗口减半，进入拥塞避免

拥塞窗口 vs 接收窗口：发送窗口 = min(两者)
```

---

## 六、HTTP 和 HTTPS 的区别

**端口号：** HTTP 默认是 80，HTTPS 默认是 443。

**URL 前缀：** HTTP 的 URL 前缀是 `http://`，HTTPS 的 URL 前缀是 `https://`。

**安全性和资源消耗：** HTTP 协议运行在 TCP 之上，所有传输的内容都是明文，客户端和服务器端都无法验证对方的身份。HTTPS 是运行在 SSL/TLS 之上的 HTTP 协议，SSL/TLS 运行在 TCP 之上。所有传输的内容都经过加密，加密采用对称加密，但对称加密的密钥用服务器方的证书进行了非对称加密。所以 HTTP 安全性没有 HTTPS 高，但是 HTTPS 比 HTTP 耗费更多服务器资源。

![常见 HTTP 状态码](https://oss.javaguide.cn/github/javaguide/cs-basics/network/http-status-code.png)

---

## 七、HTTPS 握手过程

```text
1. 客户端发送 ClientHello（支持的加密套件 + 随机数）
2. 服务端返回 ServerHello（选定加密套件 + 随机数 + 证书）
3. 客户端验证证书（CA 签名、域名匹配）
4. 客户端生成 PreMaster Secret，用证书公钥加密发送
5. 双方用三个随机数生成会话密钥（对称加密密钥）
6. 后续通信使用对称加密（效率高）
```

**为什么用混合加密：** 非对称加密安全但慢（握手阶段传密钥），对称加密快（数据传输阶段）。

---

## 八、HTTP 版本演进

| 版本 | 特点 |
|---|---|
| HTTP/1.0 | 每次请求建立新连接，无复用 |
| HTTP/1.1 | 持久连接（keep-alive）、管道化（有队头阻塞） |
| HTTP/2.0 | 多路复用、头部压缩（HPACK）、二进制分帧、服务器推送 |
| HTTP/3.0 | 基于 QUIC（UDP），解决 TCP 队头阻塞，0-RTT 建连 |

**队头阻塞：** HTTP/1.1 一个连接同时只能处理一个请求，前面的慢请求会阻塞后面的请求。

---

## 九、GET vs POST

| 对比 | GET | POST |
|---|---|---|
| 语义 | 获取资源 | 提交数据 |
| 参数位置 | URL 中 | 请求体中 |
| 缓存 | 可缓存 | 默认不缓存 |
| 幂等性 | 是 | 否（重复提交有副作用） |
| 安全性 | 参数暴露在 URL/日志 | 相对安全（仍可被抓包） |

**注意：** GET 也不是绝对安全，参数会在浏览器历史、服务器日志中暴露。

---

## 十、Cookie 和 Session

```text
Cookie：
- 存储在客户端，每次请求自动携带
- 大小限制 4KB
- 可设置过期时间、HttpOnly、Secure

Session：
- 存储在服务端
- 客户端只存 SessionId（一般放 Cookie）
- 分布式场景需共享（Redis）

对比：
- Cookie 存客户端，Session 存服务端
- Session 更安全，但服务器压力大
```

---

# 第二部分：操作系统

## 一、线程和进程的区别

**资源所有权：** 进程是资源分配的基本单位，拥有独立的地址空间；而线程是 CPU 调度的基本单位，几乎不拥有系统资源，只保留少量私有数据（PC、栈、寄存器），主要共享其所属进程的资源。

**开销：** 创建或销毁一个工厂（进程）的开销很大，需要分配独立的资源。而雇佣或解雇一个工人（线程）的开销就小得多。同理，进程间的上下文切换开销远大于线程间的切换。

### 线程同步方式

线程同步是两个或多个共享关键资源的线程的并发执行。应该同步线程以避免关键的资源使用冲突。

1. **互斥锁（Mutex）**：采用互斥对象机制，只有拥有互斥对象的线程才有访问公共资源的权限。因为互斥对象只有一个，所以可以保证公共资源不会被多个线程同时访问。比如 Java 中的 `synchronized` 关键词和各种 `Lock` 都是这种机制。
2. **读写锁（Read-Write Lock）**：允许多个线程同时读取共享资源，但只有一个线程可以对共享资源进行写操作。
3. **信号量（Semaphore）**：它允许同一时刻多个线程访问同一资源，但是需要控制同一时刻访问此资源的最大线程数量。
4. **屏障（Barrier）**：屏障是一种同步原语，用于等待多个线程到达某个点再一起继续执行。当一个线程到达屏障时，它会停止执行并等待其他线程到达屏障，直到所有线程都到达屏障后，它们才会一起继续执行。比如 Java 中的 `CyclicBarrier` 是这种机制。
5. **事件（Event）/ Wait-Notify**：通过通知操作的方式来保持多线程同步，还可以方便的实现多线程优先级的比较操作。

### 进程状态

我们一般把进程大致分为 5 种状态，这一点和线程很像：

- **创建状态（new）**：进程正在被创建，尚未到就绪状态。
- **就绪状态（ready）**：进程已处于准备运行状态，即进程获得了除了处理器之外的一切所需资源，一旦得到处理器资源（处理器分配的时间片）即可运行。
- **运行状态（running）**：进程正在处理器上运行（单核 CPU 下任意时刻只有一个进程处于运行状态）。
- **阻塞状态（waiting）**：又称为等待状态，进程正在等待某一事件而暂停运行，如等待某资源为可用或等待 IO 操作完成。即使处理器空闲，该进程也不能运行。
- **结束状态（terminated）**：进程正在从系统中消失。可能是进程正常结束或其他原因中断退出运行。

![进程状态图转换图](https://oss.javaguide.cn/github/javaguide/cs-basics/operating-system/state-transition-of-process.png)

---

## 二、虚拟内存

**虚拟内存（Virtual Memory）** 是计算机系统内存管理非常重要的一个技术，本质上来说它只是逻辑存在的，是一个假想出来的内存空间，主要作用是作为进程访问主存（物理内存）的桥梁并简化内存管理。

**隔离进程**：物理内存通过虚拟地址空间访问，虚拟地址空间与进程一一对应。每个进程都认为自己拥有了整个物理内存，进程之间彼此隔离，一个进程中的代码无法更改正在由另一进程或操作系统使用的物理内存。

**提升物理内存利用率**：有了虚拟地址空间后，操作系统只需要将进程当前正在使用的部分数据或指令加载入物理内存。

**简化内存管理**：进程都有一个一致且私有的虚拟地址空间，程序员不用和真正的物理内存打交道，而是借助虚拟地址空间访问物理内存，从而简化了内存管理。

**多个进程共享物理内存**：进程在运行过程中，会加载许多操作系统的动态库。这些库对于每个进程而言都是公用的，它们在内存中实际只会加载一份，这部分称为共享内存。

**提高内存使用安全性**：控制进程对物理内存的访问，隔离不同进程的访问权限，提高系统的安全性。

**提供更大的可使用内存空间**：可以让程序拥有超过系统物理内存大小的可用内存空间。这是因为当物理内存不够用时，可以利用磁盘充当，将物理内存页（通常大小为 4 KB）保存到磁盘文件（会影响读写速度），数据或代码页会根据需要在物理内存与磁盘之间移动。

---

## 三、进程间通信

进程间通信主要有 7 种核心方式，按场景分：

一是简单本地通信，用匿名管道（父子进程）、FIFO（无亲缘）；
二是结构化通信，用消息队列（按类型传数据）；
三是高效通信，用共享内存（最快，需配信号量同步）；
四是同步控制，用信号量（控顺序）、信号（简单通知）；
五是跨网络通信，用 Socket（唯一支持跨主机）。

选择时看需求：传大数据用共享内存，跨网络用 Socket，简单通信用管道，同步用信号量，这样能匹配效率和场景。

---

## 四、死锁

```text
四个必要条件：
1. 互斥：资源独占
2. 持有并等待：持有资源同时等待其他资源
3. 不可剥夺：资源不能被强制抢占
4. 循环等待：形成资源等待环

预防：破坏任一条件（如一次性申请所有资源）
避免：银行家算法
检测：资源分配图 + 超时机制
恢复：回滚、强制释放、杀进程
```

---

## 五、进程调度算法

| 算法 | 原理 | 特点 |
|---|---|---|
| FCFS 先来先服务 | 按到达顺序执行 | 简单，长任务阻塞短任务 |
| SJF 短作业优先 | 执行时间最短优先 | 平均等待时间短，长任务饥饿 |
| RR 时间片轮转 | 轮流执行固定时间片 | 公平，交互性好 |
| 优先级调度 | 高优先级先执行 | 灵活，低优先级饥饿 |
| 多级反馈队列 | 多队列 + 时间片 + 降级 | 现代 OS 主流（兼顾长短任务） |

---

## 六、页面置换算法

| 算法 | 原理 | 特点 |
|---|---|---|
| FIFO | 淘汰最先进入的页 | 简单，Belady 异常 |
| LRU | 淘汰最久未使用的页 | 效果好，实现成本较高 |
| LFU | 淘汰使用频率最低的页 | 适合热点保护 |
| Clock（NRU） | 环形扫描 + 访问位 | LRU 近似，成本低 |

---

## 七、select / poll / epoll

| 对比 | select | poll | epoll |
|---|---|---|---|
| 底层 | 数组（1024 上限） | 链表（无上限） | 红黑树 + 就绪链表 |
| 拷贝 | 每次调用全量拷贝 fd | 同 select | 事件注册一次，无需反复拷贝 |
| 通知方式 | 遍历所有 fd | 遍历所有 fd | 只返回就绪 fd |
| 复杂度 | O(n) | O(n) | O(1) |

**epoll 两种模式：**

```text
LT（水平触发）：只要 fd 可读就一直通知（默认）
ET（边缘触发）：状态变化才通知，需一次性读完
```

---

## 八、I/O 模型

**BIO：** 采用阻塞式 I/O 模型，线程在执行 I/O 操作时被阻塞，无法处理其他任务，适用于连接数较少的场景。

**NIO：** 采用非阻塞 I/O 模型，线程在等待 I/O 时可执行其他任务，通过 Selector 监控多个 Channel 上的事件，适用于连接数多但连接时间短的场景。

**AIO：** 使用异步 I/O 模型，线程发起 I/O 请求后立即返回，当 I/O 操作完成时通过回调函数通知线程，适用于连接数多且连接时间长的场景。

---

## 九、僵尸进程和孤儿进程

```text
僵尸进程：
- 子进程已退出，父进程未调用 wait 回收
- 占用进程号资源，不占内存
- 解决：父进程 wait，或忽略 SIGCHLD

孤儿进程：
- 父进程先退出，子进程还在运行
- 由 init（systemd）收养，自动回收
```

---

## 十、零拷贝

```text
传统 IO：磁盘 → 内核缓冲区 → 用户缓冲区 → Socket 缓冲区 → 网卡
（4 次拷贝、4 次上下文切换）

零拷贝：磁盘 → 内核缓冲区 → Socket 缓冲区 → 网卡
（内核内直接转发，减少用户态拷贝）

实现：mmap、sendfile（Kafka 就是用了 sendfile）
```
