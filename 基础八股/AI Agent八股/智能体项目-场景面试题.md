# 智能体项目模拟面试（基于简历定制）

> 生成日期：2026-09-14
> 视角：资深 Agent 面试官
> 使用说明：参考回答为答题框架，【需核实】标记处须按真实项目细节补充，切勿背诵模板式数据。

---

## 面试官视角：Agent 岗会怎么考你

```text
考察主线：你简历上有 3 段 Agent 相关落地 + 2 段 Agent 科研
1. 追问"闭环"：简历反复出现"闭环"，必问闭环的每一步怎么实现
2. 追问"效果"：每个数字（30%、25%、35%、50%、26%）都会追问怎么测的
3. 追问"失败"：工具失败、模型幻觉、链路中断怎么兜底
4. 追问"选型"：为什么用 FastGPT/AgentScope/Verl，而不用别的
5. 追问"深度"：GRPO 奖励函数、RRF 融合、状态机这些词都往细节问
```

---

# 第一部分：智能客服平台（2026.04-2026.09）

## Q1. 双路召回 + RRF 融合具体怎么设计的？

**参考回答框架：**

```text
目标：解决关键词检索漏召回、语义检索不精准的问题

双路：
- 精确路：关键词/ES 检索，保底命中专有名词、编号类问题
- 语义路：Embedding 向量检索，召回同义改写问题

融合：
- RRF（Reciprocal Rank Fusion）：score = Σ 1/(k + rank)
- 不依赖分数绝对值，只依赖排名，天然适合异源融合
- k 值（如 60）可调

效果：检索准确率提升 25%【需核实：评测集、口径】
```

**追问预判：**
- 为什么选 RRF 而不是分数加权？→ 两路分数量纲不同，加权需调参，RRF 无参数化更稳
- 单路召回 Top 多少进融合？【需核实】

## Q2. Badcase 治理闭环的"AI 归因"怎么做的？

**参考回答框架：**

```text
流水线：采集 → AI 归因 → 审核 → 语料修复 → 验证上线

AI 归因：把低质问答用 LLM 分类根因，例如：
- 知识缺失（库中无答案）
- 检索失败（有知识但没召回）
- 生成幻觉（召回了但答错）
- 意图识别错（问 A 答 B）

归因决定修复动作：缺知识→补语料；检索失败→调 chunk/query 改写；幻觉→调 prompt/重排

效果：归因效率 +35%，治理周期 周→天【需核实：归因准确率是否人工抽检】
```

**追问预判：**
- LLM 归因错了怎么办？→ 人工审核环节兜底，归因结果只是初筛
- 怎么防止"修复 A 问题引入 B 问题"？→ 验证环节回归测试集

## Q3. 快捷工单：ASR + LLM 抽取字段，怎么保证抽取准确？

**参考回答框架：**

```text
链路：通话/对话记录 → ASR 转写 → LLM 结构化抽取（JSON Schema/Function Calling）→ 预填工单 → 人工确认

保证准确的三个手段：
1. 结构化输出约束：字段枚举 + 必填校验
2. 低置信度回退：关键字段缺失/置信度低时保留人工确认
3. 抽错可改：预填而非直发，客服最终确认才建单

效果：建单效率 +50%【需核实：是端到端时长还是操作步骤数】
```

---

# 第二部分：广告智能体（2026.01-2026.04）

## Q4. "搭建-监控-优化"闭环，每个环节的 Agent 边界是什么？

**参考回答框架：**

```text
搭建：LLM 意图识别解析运营指令（自然语言 → 结构化参数）
     批量创建"计划→分组→创意"三层结构，减少手动操作 30%
监控：实时聚合多平台消耗/ROI → 规则引擎触发告警
优化：自动生成调价、换素材策略 → 推送运营确认

关键设计：优化策略"推运营确认"而非自动执行
→ 涉及投放预算的写操作必须人工确认，Agent 只做建议生成
```

**追问预判：**
- 为什么"优化"环节不自动化？→ 预算写操作风险高 + 平台 API 无幂等保证
- 规则引擎和 LLM 怎么分工？→ 数值阈值用规则（可解释、低延迟），语义判断用 LLM

## Q5. 基于 AgentScope 开发，它的核心抽象是什么？为什么不自己写循环？

**参考回答框架：**

```text
AgentScope 提供：Agent 基类、消息传递机制、多 Agent 编排、工具注册

用它解决的问题：消息路由（Agent 间消息传递）、多 Agent 生命周期管理

如果从零写：需要自己实现消息总线、状态管理、并发调度，工作量大
```

**追问预判：**
- 它的消息机制和 LangGraph 的图状态机有什么本质区别？【需核实：自己对比结论】
- AgentScope 的什么限制让你最难受？→ 生态相对小、社区资料少，遇到问题要读源码

## Q6. 多平台数据聚合，实时性和一致性怎么保证？

**参考回答框架：**

```text
多平台（如巨量/腾讯广告）数据源聚合：
- 定时拉取各平台报表 API → 归一化 → 统一指标口径
- 频率：监控类分钟级即可【需核实】
- 一致性：以平台 API 为准，本地只做聚合展示
```

---

# 第三部分：运维闭环（2025.07-2025.12）

## Q7. 为什么用 FastGPT 工作流而不是让 LLM 自由发挥（ReAct）？

**参考回答框架：**

```text
场景：网络/磁盘故障的检测→诊断→修复→工单，链路固定
固定流程用 Workflow：
- 每个节点的输入输出可预期、可校验
- 节点失败可单独重试，不用整条重跑
- 合规性：修复动作必须按固定步骤走

ReAct 适用：目标不明确、需要动态决策的场景
```

## Q8. "闭环状态机"怎么设计的？节点失败怎么办？

**参考回答框架：**

```text
状态：检测 → 诊断 → 修复 → 工单，每阶段状态机驱动
状态集：PENDING/RUNNING/SUCCESS/FAILED/WAITING_CONFIRM

失败处理：
- 可重试节点：限次重试
- 不可自动恢复：状态挂起 + 人工介入
- 状态持久化：服务重启后从断点恢复，不整条重跑

效果：工单创建 3 分钟 → 15 秒【需核实：口径是人工建单时长 vs 全流程时长】
```

**追问预判：**
- 修复动作是自动执行还是人工确认？→ 涉及生产环境变更必须确认【需核实】
- 状态机是自研还是框架？→ 自研，因为需要和工单系统耦合

---

# 第四部分：MedV-Agent（科研，2026.04-至今）

## Q9. GRPO 训练 Agent 的奖励函数怎么设计的？

**参考回答框架：**

```text
多任务统一奖励 = 任务指标 + 工具使用效率：
- 分类：准确率
- 检测：mAP
- 分割：IoU/Dice
- 工具选择 bonus：正确选工具加分，冗余工具调用减分

目的：让模型学会"少交互、高精度"的多轮策略
效果：交互步数减少 26%【需核实：对比基线是什么策略】
```

**追问预判：**
- 为什么用 GRPO 而不是 PPO？→ GRPO 去掉 critic 模型，省显存、训得快
- 奖励稀疏问题怎么处理？→ 过程奖励 + 最终奖励结合【需核实：论文中的具体做法】

## Q10. Verl 框架的 SFT→RL 管线，你改了什么？

**参考回答框架：**

```text
基于 Verl 的收益：RLHF 基础设施（rollout、reward、训练循环）不用重写
自定义的部分：
- 医学多任务数据格式与 rollout 环境
- 多模态（Qwen3-VL）的输入封装
- 自定义奖励函数与工具调用环境
【需核实：具体改了 Verl 哪个模块】
```

## Q11. 医学 Agent 的工具调用和普通 Agent 有什么区别？

**参考回答框架：**

```text
1. 工具是模型（分割头/检测头），不是 API
2. 多轮交互：点提示、框提示逐步细化，交互步数直接影响临床效率
3. 评价维度多：精度 + 交互效率 + 临床合理性（定位表达是否符合临床习惯）
```

---

# 第五部分：通用 Agent 高频追问（必准备）

## Q12. 如果 Agent 工具调用失败，你的健全运行体系是什么？

```text
1. 调用前：参数 Schema 校验 + 权限确认 + 超时上限
2. 调用中：区分错误类型——参数错（反馈给 LLM 修正）vs 网络抖动（限次重试）vs 持续故障（熔断降级）
3. 调用后：写操作结果未知时禁止自动重试，转人工对账
4. 全局：最大步数/重复调用检测/统一 traceId 日志
```

## Q13. Workflow 和 ReAct 怎么选？

```text
Workflow：链路固定、需要可预期性和合规性（你的深信服场景）
ReAct：任务开放、需要动态决策
工程上：主干 Workflow + 节点内嵌 LLM 小步决策，是常见折中
```

## Q14. 上下文过长怎么处理？

```text
1. 预算控制：Token 计数 + 超限截断策略
2. 历史压缩：旧轮次摘要化（LLM 摘要/结构化提取）
3. 记忆分层：短期（最近几轮原文）+ 长期（向量库/用户偏好 KV）
4. 效果验证：压缩后任务完成率、关键信息保留率
```

## Q15. 评测怎么做才可信？

```text
- 评测集来源：线上 badcase + 人工标注 + 历史抽样
- 分层指标：召回（Recall@K/MRR）→ 生成（准确率/相关性）→ 业务（建单时长/命中率）
- 可靠性：固定测试集版本、回归跑批、A/B 灰度
- 你简历里的每个提升数字，面试官都会问"在什么评测集上测的"【需核实全部口径】
```

## Q16. Skill 渐进式加载了解吗？

```text
Skill = 可复用的能力包（指令 + 工具 + 示例）
渐进式加载：不在系统提示词里塞全部 Skill，而是：
- 一级：Skill 清单（名称+一句话描述）供 LLM 选择
- 二级：选定后动态注入完整指令
好处：省 Token、减少指令混淆、可扩展
```

---

# 第七部分：大模型评测与智能体评测八股

## 先背这一句

> 大模型评测重点看“回答好不好”；智能体评测重点看“任务是否可靠完成”。后者除最终答案外，还必须评估规划轨迹、工具调用、成本时延、异常恢复和安全边界。

## Q17. 如何搭建一套大模型评测体系？

```text
先定义任务和验收标准，再构建评测集；离线评测用于快速迭代，线上 A/B 验证真实业务收益，最后通过 badcase 回流形成闭环。

典型分层：
1. 基础能力：指令遵循、格式正确、拒答安全
2. 业务答案：正确性、相关性、完整性、事实一致性
3. RAG 链路：召回、重排、生成分别评测
4. 线上业务：任务完成率、人工介入率、满意度、成本和延迟
```

## Q18. 大模型常用评测指标有哪些？

| 维度 | 常见指标 | 说明 |
|---|---|---|
| 正确性 | Accuracy、EM、F1、人工通过率 | 有标准答案的任务优先用确定性指标 |
| 相关性 | Relevance Score | 回答是否切中用户问题 |
| 完整性 | Completeness Score | 是否遗漏关键条件、步骤或限制 |
| 事实一致性 | Faithfulness / Groundedness | 回答是否能被给定上下文或证据支撑 |
| 幻觉 | Hallucination Rate | 无依据编造、事实错误的比例 |
| 指令遵循 | Format / Constraint Pass Rate | JSON、字段、长度、禁用项等是否符合要求 |
| 安全性 | Unsafe Output Rate、Attack Success Rate | 越权、泄露、提示注入等风险比例 |
| 性能成本 | P50/P95 延迟、Token、单任务成本 | 生产环境必须纳入选型 |

**回答要点：** 开放式生成不适合只看 BLEU、ROUGE；它们更适合文本与参考答案表面重合度高的场景。业务问答更应看正确性、事实一致性和人工/LLM Judge 评分。

## Q19. 评测集如何构建，怎样保证可信？

```text
来源分三类：
- 真实线上问题和 badcase：代表真实分布
- 领域专家/运营人工构造：覆盖关键业务规则
- 对抗和边界样本：歧义问题、长文本、注入攻击、工具故障等

保证可信：
1. 明确题目、参考答案、证据和评分标准
2. 训练集、调参集、测试集严格隔离，防止评测泄漏
3. 按场景分桶：高频、长尾、高风险、不同用户群体分别看
4. 固化版本和数据快照，每次变更跑同一套回归集
5. 关键样本双人标注或专家复核，记录标注一致性
```

## Q20. LLM-as-a-Judge 是什么？如何避免它不可靠？

```text
LLM-as-a-Judge 是让一个评审模型按预设 Rubric 对回答打分或做两两比较。

正确用法：
- 先定义清晰 Rubric，例如正确性/完整性/事实一致性各 1~5 分
- 给评审模型问题、上下文、标准答案和待评答案，不只给待评答案
- 优先做盲评和两两比较，并随机交换 A/B 顺序
- 抽样由人工专家复核，与人工结果计算一致性
- 对关键结论同时报告自动评测和人工评测

常见偏差：偏好更长答案、位置偏差、偏好同源模型、自信但错误的评分。因此 LLM Judge 不能替代人工，只能提高评测吞吐。
```

## Q21. RAG 应该怎样分层评测？

```text
不能只看最终答案，要拆成三层：

1. 召回层：Recall@K、MRR、nDCG
   看正确文档是否进入 TopK、排位是否靠前。
2. 重排层：Precision@K、nDCG
   看相关文档是否被排到前面。
3. 生成层：Answer Correctness、Faithfulness、Citation Correctness
   看答案是否正确、是否依据召回内容、引用是否准确。

如果最终答案差，先定位是“没召回”“排得靠后”还是“召回了但模型没用好”，再决定优化 Embedding、Reranker、Chunk、Prompt 还是模型。
```

## Q22. 如何评估幻觉？

```text
先定义幻觉：回答中的关键事实无法被给定上下文、可信工具结果或标准答案支持。

方法：
1. 把回答拆成原子事实（claim）
2. 对每条事实判断“有证据支持 / 无证据支持 / 与证据矛盾”
3. 计算无支持或矛盾事实占比，即幻觉率

生产上还要区分：
- 有知识但没引用：证据使用问题
- 知识库没有但模型编造：应明确说“资料不足”
- 工具结果过期：数据时效性问题
```

## Q23. 智能体评测和普通大模型评测的核心区别？

| 维度 | 大模型评测 | 智能体评测 |
|---|---|---|
| 关注对象 | 单次输入到输出的质量 | 多步任务是否完成及执行过程 |
| 输入 | Prompt、上下文 | Prompt、状态、工具、环境反馈 |
| 关键指标 | 正确性、相关性、事实性 | 任务成功率、工具成功率、轨迹质量、恢复能力、安全性 |
| 难点 | 开放式答案主观 | 环境不稳定、工具有副作用、轨迹可多解 |
| 典型方法 | 金标答案、人工/LLM Judge | 沙箱回放、轨迹评分、端到端验收、故障注入 |

## Q24. Agent 的核心评测指标是什么？

| 层级 | 指标 | 说明 |
|---|---|---|
| 结果 | Task Success Rate | 是否达到预定义验收条件，而非“回答看起来合理” |
| 规划 | Plan Validity、Subgoal Completion | 计划是否可执行，各子任务是否完成 |
| 工具 | Tool Selection Accuracy、Argument Validity、Tool Success Rate | 工具选得对不对、参数是否合法、调用是否成功 |
| 效率 | 平均步骤数、重复调用率、Token、单任务成本、P95 延迟 | 同样成功前提下越短、越稳、越省越好 |
| 稳定性 | Retry Success Rate、Recovery Rate、Loop Rate | 工具失败后能否恢复，是否陷入循环 |
| 安全 | Permission Compliance、Unsafe Action Rate、Prompt Injection ASR | 是否越权、危险操作是否拦截、注入攻击是否得逞 |

**最关键的口径：**

```text
Task Success Rate = 通过验收的任务数 / 总任务数

验收条件必须事先写清，例如：
“成功创建工单”不等于 Agent 说“已创建”，而是工单系统中存在唯一且字段正确的工单记录。
```

## Q25. 为什么 Agent 不能只评最终答案？

```text
因为最终答案正确，过程也可能有风险：
- 调错工具后碰巧得到正确结果
- 重复调用导致成本过高
- 对写操作没有权限确认
- 先泄露敏感信息再完成任务

所以 Agent 至少同时评“结果正确性 + 轨迹合理性 + 安全边界 + 效率”。
```

## Q26. Agent 轨迹怎么评测？

```text
轨迹并不要求和参考轨迹完全一致，因为同一任务可能有多条正确路径。

评测方式：
1. 硬规则：工具白名单、参数 Schema、权限、最大步数、禁止危险动作
2. 过程检查点：关键子目标是否完成，例如“先查询订单再退款”
3. 结果验收：外部环境状态是否正确
4. LLM/人工 Judge：对开放式规划评估是否合理、是否存在明显冗余

原则：优先评“是否满足约束并完成目标”，不要机械要求与单一路径逐 token 一致。
```

## Q27. Agent 工具故障、环境波动如何评测？

```text
要做故障注入，而不是只测 Happy Path：
- 工具超时、5xx、限流
- 参数缺失或返回空结果
- 工具返回过期/冲突数据
- 用户中途补充或修改目标
- 写操作结果未知

关注：
1. 是否区分可重试和不可重试错误
2. 是否有限次数重试并避免死循环
3. 是否走备用工具、降级或转人工
4. 写操作不确定时是否先查状态，而不是盲目重试
```

## Q28. Agent 评测如何保证可复现？

```text
1. 固定模型版本、Prompt、工具版本、评测集和环境快照
2. 外部 API 用 Mock/Sandbox 或录制回放，避免线上数据变化干扰
3. 控制随机性：固定 temperature/seed；对随机任务多次运行，报告均值和方差
4. 每条任务保存 traceId、输入、工具调用轨迹、最终环境状态
5. 指标同时看总体和分桶，不只报一个平均分
```

## Q29. 多 Agent 如何评测？

```text
先看团队最终 Task Success Rate，再额外看协作成本：
- 任务分配是否正确
- Agent 间信息传递是否遗漏或冲突
- 是否发生重复工作
- 总 Token、总耗时是否比单 Agent 有收益
- 关键决策是否有统一的最终裁决者

多 Agent 不是 Agent 越多越好。如果任务本身简单，协作通信成本可能超过收益。
```

## Q30. 离线评测很好，为什么还要线上 A/B？

```text
离线集只能代表历史和构造场景，无法完整覆盖真实用户表达、流量分布和业务流程。

线上 A/B 主要验证：
- 用户任务完成率、满意度、转人工率
- 实际延迟和成本
- 新版本是否引入新的 badcase 或安全风险

上线方式：灰度 → 监控 → 抽检 → badcase 回流，不应只凭离线分数直接全量发布。
```

## Q31. 如果模型分数提升，但业务指标没提升，怎么排查？

```text
先检查三件事：
1. 离线指标和业务目标是否一致，例如通用问答分数不等于工单完成率
2. 评测集是否过于简单或与线上分布脱节
3. 是否优化了某一环但瓶颈在别处，例如召回提升但工具执行仍失败

然后按漏斗拆解：输入理解 → 检索 → 生成/规划 → 工具执行 → 最终业务状态。
```

## 高频易踩坑

```text
1. 只报“准确率提升”，不说明任务定义、样本规模、基线和置信区间
2. 只用 LLM Judge，不做人工校准
3. RAG 只看最终答案，不拆召回、重排、生成
4. Agent 只跑正常路径，不做工具超时/失败注入
5. 把“工具调用成功”当成“业务任务完成”
6. 测试集参与 Prompt 调优，导致评测泄漏
7. 只看平均值，不看高风险场景、长尾样本和 P95 延迟
```

## 面试万能回答模板

```text
我会先把业务目标转成可验收的任务定义，再用真实线上样本、人工构造样本和对抗样本建立分层评测集。

离线阶段会拆链路评测：RAG 看召回/重排/生成，Agent 看结果、工具、轨迹、效率和安全；开放式答案通过明确 Rubric 的 LLM Judge 加人工抽检评估。

上线前做固定回归集和故障注入，上线后通过灰度 A/B 观察任务完成率、人工介入率、成本和 badcase，并把 badcase 回流到下一轮优化。
```

---

## 一句话总结

> 你简历的强项是"Agent 落地闭环 + RL 训练"双重背景，弱项风险是数字口径和细节深度。面试前把每个数字的"定义+测试集+基线"过一遍，把每个"闭环"的失败处理路径想清楚，就足以应对资深 Agent 面试官的连环追问。
